name: QuickJS Windows

on:
  workflow_dispatch:
  push:
    paths:
      - "crates/shell/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/quickjs-windows.yml"
  pull_request:
    paths:
      - "crates/shell/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/quickjs-windows.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: QuickJS on i686-pc-windows-msvc
    runs-on: windows-latest
    env:
      RUST_BACKTRACE: 1
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: i686-pc-windows-msvc
      - name: Setup | Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: quickjs-i686-windows-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Build the QuickJS bindings for 32-bit Windows
        run: cargo build -p rquickjs --features macro,loader,jit-abi --target i686-pc-windows-msvc --locked --verbose
      # The 64-bit-only JIT compiler is pulled in per target by
      # `crates/shell/Cargo.toml`; 32-bit Windows gets the interpreter-only
      # runtime, so the shell crates must still compile there.
      - name: Check the shell crates for 32-bit Windows
        run: cargo check -p gpui-shell -p gpui-component-shell --target i686-pc-windows-msvc --locked --verbose
